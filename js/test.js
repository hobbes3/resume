function calculateTotal(price, tax) {
    // Syntax error: missing closing parenthesis
    if (price > 0 {
        return price + (price * tax);
    }

    // Reference error: 'discount' is undeclared
    return price - discount;
}

// Call function with missing arguments
calculateTotal(;
