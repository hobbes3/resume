name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-o: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Indentation error under 'run' block
    - name: Run Linter
      run:
     npm run lint
       npm test

    # Missing required field 'uses' or 'run'
    - name: Broken Step
